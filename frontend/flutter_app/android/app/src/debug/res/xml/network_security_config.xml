<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local HTTP is accepted only in debug builds. Release builds keep Android's HTTPS default. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
